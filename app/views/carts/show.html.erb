<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if enhanced_cart.present? %>
    <div class="panel panel-default items">
      <table class="table table-bordered">
        <!-- Existing table header, body, and footer code -->
      </table>
    </div>

    <div class="row">
      <div class="col-xs-12">
        <%= form_tag orders_path do %>
          <!-- Existing Stripe checkout script code -->
        <% end %>
      </div>
    </div>

  <% else %>
    <h2>Your cart is empty!</h2>
    <p>It looks like you haven't added any items to your cart yet. Start shopping by visiting our <a href="/">home page</a>.</p>
  <% end %>

</section>
